{
  "name": "proyecto-josemaria-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "if exist node_modules rd /s /q node_modules && if exist dist rd /s /q dist && if exist package-lock.json del package-lock.json",
    "deploy": "npm run clean && npm install && npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.2.4"
  }
}
